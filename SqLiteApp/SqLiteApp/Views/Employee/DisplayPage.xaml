<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SqLiteApp.DisplayPage">

    <ContentPage.Content>
        <StackLayout Orientation="Horizontal">
                <ListView x:Name="myListView"  HasUnevenRows="True" ItemTapped="MyListView_ItemTapped" >
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <!--<TextCell Detail="{Binding Userid}" />-->
                        <!--<TextCell Text="{Binding First Name}" TextColor="#1ea593" DetailColor="Black" Detail="{Binding Department} "/>-->
                        <TextCell Text="{Binding Userid}" TextColor="#1ea593 "/>

                    </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

            </StackLayout>
        </ContentPage.Content>
</ContentPage>